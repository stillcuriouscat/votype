# Ralph Progress Log
Started: Sun 18 May 2026 (anthropic-fallback)
---

## Codebase Patterns (carried over from previous rounds)
- vertex_proxy.py is a self-contained script on Oracle Cloud — reads JSON from stdin, calls Gemini API, writes text to stdout
- post_processor_configs.py sends JSON to vertex_proxy.py via SSH subprocess — any new stdin fields must be added to both sender (json.dumps) and receiver (data.get)
- _run_vertex_proxy() 429 fallback replaces only "model" in the payload — all other fields preserved automatically
- _log() calls in voice_input.py: ASR/PP logs no longer truncate ([:120] removed); PUNC log (line 1066) intentionally kept
- Pre-existing test failures: test_pw_record.py / test_trim_clipping.py need soundfile module; test_cli_state.py mock issue; test_ssh_claude.py/test_vertex_ai.py OpenRouter assertion mismatches
- DEFAULT_POST_PROCESSOR currently 'gemini-merge' (will change to 'claude-merge')
- _DEPRECATED_PP mapping in state_db.py handles auto-migration on get_state()
- Anthropic API key already deployed at oracle-cloud:~/.config/claude.secret (sk-ant-api03...)

## This Round (Anthropic fallback)
- Gemini Vertex AI became unreliable, switching primary to Anthropic Claude Haiku
- Anthropic SDK call pattern: `client.messages.create(model=..., max_tokens=..., system=..., messages=[{role:'user', content:...}])`
- Anthropic uses `max_tokens` (not `max_output_tokens` like Gemini SDK)
- Reuse _run_vertex_proxy() — it's actually generic (proxy_script comes from config)
- Reuse prompts (gemini-fix-system.txt, gemini-merge-system.txt — LLM-agnostic editing instructions)
- DO NOT delete existing gemini-* / haiku-fix presets
